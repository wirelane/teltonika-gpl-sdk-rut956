--- a/mt7603/eeprom.c
+++ b/mt7603/eeprom.c
@@ -162,8 +162,12 @@
 	if (ret < 0)
 		return ret;
 
+	ret = mt7603_check_eeprom(&dev->mt76);
+	if (ret < 0)
+		return ret;
+
 	if (dev->mt76.otp.data) {
-		if (mt7603_check_eeprom(&dev->mt76) == 0)
+		if (ret == 0)
 			mt7603_apply_cal_free_data(dev, dev->mt76.otp.data);
 		else
 			memcpy(dev->mt76.eeprom.data, dev->mt76.otp.data,
